"""Independently versioned macro-regime model architectures.

Every model version owns its regime definition, transition assumptions,
likelihood, inference, and evaluation primitives.  Versioned namespaces keep
future research designs from silently changing the frozen Model 01 artifact
contract or its historical results.
"""
